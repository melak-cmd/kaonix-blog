services:
  web:
    build: .
    image: kaonix-blog
    container_name: kaonix-blog
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./certs:/etc/nginx/certs:ro
    restart: unless-stopped
